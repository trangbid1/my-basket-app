repos:
  - repo: local
    hooks:
      - id: ai-guard
        name: AI Security Guard
        entry: python .git-hooks/ai-guard.py
        language: system
        stages: [pre-commit]
        pass_filenames: false
        verbose: true
