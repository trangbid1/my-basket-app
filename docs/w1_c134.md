# Week 1 - Challenge 1.3.4
# Prompt
ROLE
As a Senior QA Engineer specializing in microservices...

CONTEXT
Application: MyBasket Lite
Target Logic: createOrder in #file:microservices/order-service/src/service.ts
Endpoints: POST /api/orders(Port 3003)
TASK
Generate a comprehensive set of manual test cases for the create order logic.

CONSTRAINTS
Include Positive (happy path), Negative (invalid inputs), and Edge cases (empty results).
You must include a "Data Integrity" section in your prompt that checks if the order total matches the sum of cart items.
Format as a Markdown table with: Test ID, Description, Steps, and Expected Result.


# RESULT
## Test Cases for `createOrder`

| Test ID | Description                              | Steps                                                                                     | Expected Result                                                                 |
|---------|------------------------------------------|-------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------|
| **Positive Cases**                                                                                                                                            |
| TC001   | Create order with valid data             | 1. Send a POST request to `/api/orders` with valid `userId` and `orderData`.              | Order is created successfully with correct total amount and status `PENDING`.  |
| TC002   | Create order with multiple items         | 1. Send a POST request with multiple items in `orderData`.                                | Order is created with all items and correct total amount.                      |
| TC003   | Create order with decimal prices         | 1. Send a POST request with items having decimal prices (e.g., $10.99).                  | Total amount is rounded to two decimal places.                                 |
| **Negative Cases**                                                                                                                                            |
| TC004   | Create order with empty items array      | 1. Send a POST request with `orderData.items` as an empty array.                         | Error message: "Order must contain at least one item".                         |
| TC005   | Create order with missing `items` field  | 1. Send a POST request without the `items` field in `orderData`.                         | Error message: "Order must contain at least one item".                         |
| TC006   | Create order with invalid `userId`       | 1. Send a POST request with an invalid `userId`.                                          | Error message or HTTP 400/404 response.                                        |
| TC007   | Create order with invalid item structure | 1. Send a POST request with malformed item objects (e.g., missing `price` or `quantity`).| Error message indicating invalid item structure.                               |
| TC008   | Create order with invalid payment method | 1. Send a POST request with an unsupported `paymentMethod`.                              | Error message indicating invalid payment method.                               |
| **Edge Cases**                                                                                                                                               |
| TC009   | Create order with zero quantity items    | 1. Send a POST request with items having `quantity: 0`.                                  | Error message indicating invalid quantity.                                     |
| TC010   | Create order with extremely large values | 1. Send a POST request with very high `price` or `quantity` values.                      | Order is created successfully, and total amount is calculated correctly.       |
| TC011   | Create order with empty shipping address | 1. Send a POST request with an empty `shippingAddress`.                                  | Error message indicating invalid shipping address.                             |
| TC012   | Create order with empty billing address  | 1. Send a POST request with an empty `billingAddress`.                                   | Error message indicating invalid billing address.                              |
| **Data Integrity**                                                                                                                                           |
| TC013   | Verify total amount calculation          | 1. Send a POST request with multiple items.                                              | Total amount matches the sum of `price * quantity` for all items.              |
| TC014   | Verify cart is cleared after order       | 1. Create an order. 2. Check the cart for the same `userId`.                             | Cart is empty after order creation.                                            |
| TC015   | Verify timestamps in order               | 1. Create an order.                                                                      | `createdAt` and `updatedAt` timestamps are set to the current time.            |
| TC016   | Verify estimated delivery date           | 1. Create an order.                                                                      | `estimatedDelivery` is calculated and set correctly.                           |